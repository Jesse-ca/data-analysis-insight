---
name: data-analysis-insight
description: |
  通用数据分析洞察框架。用户上传Excel，AI自动识别场景、推断指标、生成看板。
  触发条件：用户上传数据并要求分析/生成报告/看板/洞察。
---

# 通用数据分析洞察框架

## 核心原则（写死）

1. **严禁编造原始数据**，不确定就写"数据不足"
2. **边缘情况处理**：样本量<3条，标注"数据不足，结论仅供参考"
3. 每条洞察必须有数字证据支撑

## 核心价值

**自动化你的工作流程**：你之前手动做的（识别场景 → 设计指标 → 选图表 → 写提示词），现在skill自动完成。

## 执行流程

### Step 1: 数据接收

用户上传Excel文件，skill读取并解析。

**固定解析工具：Python pandas**

```bash
# 首次使用时安装依赖
pip install pandas openpyxl

# 解析Excel文件
python scripts/parse_excel.py <excel_file_path>
```

**解析脚本位置**：`scripts/parse_excel.py`

**解析结果包含**：
- Sheet名称和数量
- 每个Sheet的行列数
- 字段名称和数据类型
- 空值统计
- 数值列统计（最小值、最大值、平均值、总和）
- 分类列统计（唯一值数量、Top10值）
- 前5行样本数据
- 完整数据（最多10000行，超过时截断并标注）

### Step 2: 场景识别（scene-recognizer）

AI分析数据，自动识别：
- 这是什么场景？（让AI自由推断）
- 数据结构是什么？（单表/多表/双表）
- 有哪些字段？字段类型是什么？
- 数据规模如何？（行数、字段数、时间范围）

### Step 3: 指标推断（metric-inferencer）

基于识别的场景，AI推断：
- 应该计算哪些KPI？
- 每个KPI的计算公式是什么？
- 应该按什么维度分析？

**原则**：
- 只计算原子指标，不计算汇总指标
- 每个KPI必须有明确的计算公式
- 公式必须说明：分子是什么、分母是什么

### Step 4: 图表选型（chart-selector）

根据数据特征，自动选择图表（规则写死）：
- 时间序列 ≤5点 → 柱状图
- 时间序列 6-12点 → 折线图
- 时间序列 >12点 → 面积图
- 类别对比 ≤5类 → 柱状图
- 类别对比 >5类 → 横向条形图
- 占比分析 ≤6类 → 饼图
- 占比分析 >6类 → 环形图
- 2分类+1数值 → 热力图
- 3维度 → 气泡图

**维度分析数量要求（智能决定）**：
- 单维度分析：覆盖数据中所有可用维度
- 组合维度分析：根据维度数量动态决定
  - 3个维度以下：≥2个组合维度
  - 3个维度以上：≥3个组合维度
- **不设硬性数量限制，以"有分析价值"为准**

### Step 5: 流程编排（flow-orchestrator）

根据场景复杂度，选择分析流程：

**默认规则（写死）**：
- **默认走PDCAWFR**（深度分析）
- **只有用户明确说"生成看板工具"或"做HTML工具"**，才走Tool模式

**看板 vs 工具（写死）**：

| 概念 | 定义 | 数据来源 | 交互性 | 适用场景 |
|------|------|---------|--------|---------|
| **看板** | 静态报告 | 数据已嵌入HTML | 无交互，打开即看 | "分析数据"、"生成报告"、"做个看板"、"分析一下" |
| **工具** | 交互式工具 | 用户上传Excel | 实时计算，有交互 | "生成看板工具"、"做HTML工具"、"做个交互式工具" |

| 用户说法 | 流程 | 输出载体 | 洞察与根因分析方式 |
|----------|------|---------|-------------------|
| "分析数据"、"生成报告"、"做个看板"、"分析一下" | PDCAWFR（默认） | **静态HTML报告**（数据已嵌入，打开即看） | **AI在线分析**：AI智能选择方法（5 Whys/鱼骨图/帕累托等），给出方法选择依据、判断标准、反例检验 |
| "生成看板工具"、"做HTML工具"、"做个交互式工具" | Tool | **交互式HTML工具**（用户上传Excel，实时计算） | **离线规则驱动**：使用8类固定规则（极值/集中度/趋势/异常/关联/偏离/对比/行动建议），**禁止WFR分析** |

**PDCAWFR流程**：
- P：需求拆解
- D：数据执行
- **C：逻辑自检（强制执行，不可跳过）**
  - 总量校验：各分类加总 = KPI总计
  - 公式校验：分子分母、单位、小数位数
  - 极端值校验：异常极值检查
- **W：情景模拟（AI智能判断是否需要）**
  - 判断依据：C阶段有异常 + 维度≥3
  - 或用户主动指定模拟目标
- **F：趋势预测（AI智能判断是否需要）**
  - 判断依据：有≥2期数据 + 有时间维度
- **R：根因诊断（AI在线分析，智能判断是否需要）**
  - 判断依据：C阶段有异常 / 维度≥2
  - AI智能选择方法（5 Whys/鱼骨图/帕累托等）
  - 必须给出：方法选择依据、判断标准、量化数据、反例检验、边界条件、置信度
- **A：异常处理与行动建议**
  - 边缘情况处理
  - 洞察提炼
  - 行动建议（必须包含量化预期）

### Step 6: 生成提示词

组装完整的分析提示词，包含：
- 场景描述
- 数据结构
- KPI定义
- 图表选型
- 分析流程

### Step 7: AI执行

将提示词提交给AI执行，生成HTML看板。

### Step 8: 输出

用户获得完整的HTML分析报告。

## 用户配置（可选）

用户可以提供配置文件，覆盖默认行为：

| 配置文件 | 作用 |
|----------|------|
| 框架文件 | 自定义分析流程（PDCA/PDCAWFR） |
| 特殊规则 | 自定义KPI公式、阈值等 |
| 模板文件 | 自定义HTML输出格式 |

**不需要配置**：上传Excel就能用，configs/是可选的。

## 技术栈

- ECharts CDN：图表渲染
- SheetJS CDN：Excel解析
- 纯JavaScript：业务逻辑

## 目录结构

```
data-analysis-insight/
├── SKILL.md                          # 主文件
├── modules/                          # 核心模块（AI执行逻辑）
│   ├── scene-recognizer.md           # 场景识别
│   ├── metric-inferencer.md          # 指标推断
│   ├── chart-selector.md             # 图表选型（规则写死）
│   ├── flow-orchestrator.md          # 流程编排
│   ├── html-builder.md               # HTML组装
│   └── data-storytelling.md          # 数据故事结构
├── configs/                          # 用户配置（可选）
│   └── README.md
├── references/                       # 参考文档
│   ├── README.md
│   └── reference-materials.md        # 参考材料（KPI库、图表指南）
└── docs/                             # 使用文档
    ├── USAGE.md                      # 使用说明
    ├── troubleshooting.md            # 故障排除
    └── success-criteria.md           # 成功标准
```
